version: '3.8'

services:
  detective-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ../.env 
    volumes:
      - .:/app
      - ./static:/app/static
    restart: unless-stopped